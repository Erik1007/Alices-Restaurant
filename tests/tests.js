describe("Test suite", function () {
    it("should return the expected result", function () {
        expect(myFunction()).toEqual("expected result");
    });
});

describe('modal-confirmation-button', onclick() {
    it("should connect the confirm button to the success html", onclick() {
        expect(document.getElementById()).toEqual('/reservation/success.hmtl');
    });
});